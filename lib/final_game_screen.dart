import 'package:flutter/material.dart';

class FinalGameScreen extends StatelessWidget {
  const FinalGameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Row(children: [Text('Lið'), Text('Vs'), Text('LIÐ')]),
      ],
    );
  }
}
